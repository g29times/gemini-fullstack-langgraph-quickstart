# AI招投标产品上线检查清单 (Go-Live Checklist)

---

**目标**：确保AI招投标产品所有组件在上线前经过全面检查、功能完备、性能稳定，并获得各负责人确认。

**上线日期**：`YYYY-MM-DD`

---

## 一、 系统组件检查点

### 1. RAG 服务 (REST API)

| 检查项 | 负责人 | 状态 (✔️/❌) | 备注 |
| :--- | :--- | :--- | :--- |
| **1.1 生产环境API Endpoint可用性** | 后端 | | `http://www.raritag.cn/intelligence-platform/bidProject/search` |
| **1.2 生产环境认证Token配置** | 后端 | | 确认Bearer Token有效 |
| **1.3 API性能与延迟（P95）** | 后端/运维 | | 压力测试结果是否在可接受范围（如 < 500ms） |
| **1.4 数据更新与同步机制** | 后端 | | 确认线上RAG数据源是最新的 |
| **1.5 查询格式兼容性** | 后端 | | 支持`personalized.rag`和`canonical`查询格式 |
| **1.6 返回数据结构校验** | 后端/测试 | | `{"success": true, "data": [...]}` 结构稳定 |
| **1.7 Fallback机制** | 后端 | | 接口异常时，系统能否优雅降级（如到Web Search） |
| **1.8 日志与监控** | 运维 | | 生产环境API的请求/响应/错误日志已接入监控系统 |

### 2. 线上数据库数据导入

| 检查项 | 负责人 | 状态 (✔️/❌) | 备注 |
| :--- | :--- | :--- | :--- |
| **2.1 用户项目数据源确认** | 后端 | | 确认生产环境用户项目数据的来源库和表 |
| **2.2 个性化推荐接口可用性** | 后端 | | `getRecommendWordByUser` 接口在生产环境可用 |
| **2.3 数据迁移脚本** | 运维/后端 | | 脚本经过测试，可重复执行 |
| **2.4 数据完整性与一致性校验** | 测试/后端 | | 抽样检查线上数据与源数据是否一致 |
| **2.5 增量同步机制** | 后端 | | 如有，确认增量同步逻辑正常工作 |
| **2.6 权限与访问控制** | 运维 | | 确认生产环境数据库访问权限最小化原则 |

### 3. LangGraph 流程引擎服务 (本项目)

| 检查项 | 负责人 | 状态 (✔️/❌) | 备注 |
| :--- | :--- | :--- | :--- |
| **3.1 生产环境`.env`配置** | 后端/运维 | | 所有API Keys, Endpoints, Tokens已替换为生产环境配置 |
| **3.2 依赖项与虚拟环境** | 运维 | | `requirements.txt` 内所有依赖已在生产环境安装 |
| **3.3 Prompts最终版本确认** | 后端 | | `prompts.py` 中所有提示词已是最新优化版本 |
| **3.4 Agent图谱与状态机** | 后端 | | `graph.py`, `state.py` 逻辑稳定，无未修复bug |
| **3.5 个性化查询逻辑** | 后端/测试 | | 用户项目注入、查询配比 (`personalization_query_ratio`) 正常 |
| **3.6 混合重排与延迟调用** | 后端/测试 | | `defer_api_rerank_to_reflection` 等重排策略符合预期 |
| **3.7 Web/RAG双队列派发** | 后端/测试 | | Topic+Project查询派发逻辑在各种场景下正常工作 |
| **38 Thinking Process传递** | 后端/测试 | | 思考过程能否完整、正确地在各节点间传递 |
| **3.9 错误处理与重试** | 后端 | | 节点执行失败时，重试和错误处理机制是否健全 |
| **3.10 日志级别与格式** | 运维/后端 | | 生产环境日志级别设置为`INFO`，格式标准化 |

### 4. 前端服务

| 检查项 | 负责人 | 状态 (✔️/❌) | 备注 |
| :--- | :--- | :--- | :--- |
| **4.1 API Endpoint指向生产环境** | 前端 | | 所有对后端的API请求均指向生产域名 |
| **4.2 用户认证与会话管理** | 前端 | | 登录、登出、会话超时处理正常 |
| **4.3 WebSocket连接** | 前端/后端 | | `stream_events` 事件流接收正常，连接稳定 |
| **4.4 报告渲染与展示** | 前端/测试 | | 最终报告、引用、思考过程等内容能正确渲染 |
| **4.5 跨浏览器兼容性** | 前端/测试 | | Chrome, Edge, Safari等主流浏览器显示和功能正常 |
| **4.6 移动端适配** | 前端/测试 | | 在主流移动设备上布局和交互体验良好 |
| **4.7 性能（首次加载/可交互时间）** | 前端/运维 | | 页面加载性能达标 |
| **4.8 用户行为埋点** | 前端 | | GA/Clarity等分析工具已配置并正常工作 |

---

## 二、 团队角色签名

请在确认完成上述所有相关检查项后签名。签名即代表您已确认您负责的部分已准备就绪，可以上线。

| 角色 | 签名 | 日期 |
| :--- | :--- | :--- |
| **后端负责人 (Backend Lead)** | | |
| **前端负责人 (Frontend Lead)** | | |
| **测试负责人 (QA Lead)** | | |
| **运维负责人 (Ops Lead)** | | |
| **项目负责人 (Project Leader)** | | |

---

**上线授权**：所有角色签名完毕后，由项目负责人最终确认，授权上线操作。
